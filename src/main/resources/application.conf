UserApp {
  dataSourceClassName = org.postgresql.ds.PGSimpleDataSource
  dataSource {
    user = postgres
    portNumber = 5432
    password = password
  }
}

TweetApp {
  maximumPoolSize=10
  dataSourceClassName = org.postgresql.ds.PGSimpleDataSource
  dataSource {
    user = postgres
    portNumber = 5432
    password = password
  }
}
